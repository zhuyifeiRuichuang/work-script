# 2026-07-09 Work Log

## NameNode HA (Plan B) Implementation — Complete
- Rewrote `namenode.yaml`: 2 replicas (Active/Standby), ZKFC sidecar, complex HA init sequence
  - initContainer: nn0 → formatZK + format NN + initializeSharedEdits; nn1 → wait nn0 RPC + bootstrapStandby
  - ZKFC sidecar: waits for local NN RPC then starts `hdfs zkfc`
  - Hard podAntiAffinity (2 NNs on different nodes)
  - Single PVC per Pod (openebs-hostpath, not Longhorn)
  - PDB minAvailable=1
- Updated `datanode.yaml`: initContainer now checks nn1 OR nn2 (HA-aware)
- Rewrote `deploy-guide.md`: complete HA architecture docs with initialization sequence, failover testing, node failure recovery, verification checklist
- Updated MEMORY.md: Plan B design decisions, component list, deployment order

## Configuration Cross-Validation — Complete
- Systematically cross-checked all 8 YAML files + deploy-guide.md
- 14/16 checks passed: ConfigMap keys, Service ports, FQDN addresses, StorageClass, images, HA init logic, anti-affinity, PDB, QOS, volume names, HA URI, deploy-guide content
- Fixed: Removed misleading `containerPort: 8019` from namenode container (port belongs to ZKFC sidecar, not NN process)
- Known limitation: `yarn.log.server.url` points to port 8188 (JobHistory Server) which is not deployed — cosmetic issue only, log aggregation still works

## Pod Management Policy Optimization
- Changed ZK/JN/DN/NM from `OrderedReady` → `Parallel` for faster startup (3 Pods start simultaneously)
- NN keeps `OrderedReady` (required: nn0 must complete HA init before nn1 bootstrapStandby)
- ZK supports arbitrary startup order — each node uses myid for identity, waits for quorum to form
- JN same — no inter-pod startup dependency, NN initContainer waits for JN ≥2/3
- DN/NM initContainers already have wait-for-dependency logic, Parallel is safe
- Updated deploy-guide.md: deployment script comments, rolling update section documentation
