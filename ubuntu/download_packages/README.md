# 说明
用于下载指定名字的软件包及直接依赖文件，间接依赖需指定包名再次下载。

支持Ubuntu发行版。



# 文件说明
`download_packages_v1.sh` 仅支持单个软件及其直接依赖包下载，每个软件是单独的一个压缩包，执行脚本可见帮助说明。  
`download_packages-v2.sh` 引入批量下载功能，读取`packages.list`中软件包名字。`packages.list`需填写想下载的软件包名字，可将多个软件打包到一个压缩包，执行脚本可见帮助说明。   